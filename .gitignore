# Build output
/bin/
/dist/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test output
*.test
*.out
*.prof
coverage.txt
coverage.html

# IDE
.vscode/
.idea/
*.swp
*.swo

# Local state (never commit these — operator-generated)
/data/
/state/
*.crt
*.key
*.pem
dek.bin
dek.prev.bin
beacon-state.bbolt*
enrollment-metadata.json
etag.txt

# Generated code (regenerated via `make generate`) — committed for hermetic CI.
# CI does not have access to the netbrain repo's OpenAPI spec, so the generated
# client is checked in. Run `make generate` after any spec change in the
# netbrain repo, then commit the regenerated file.
# internal/api/zz_generated.go  # intentionally tracked

# OS
.DS_Store
Thumbs.db

# Go workspaces (we don't use them)
go.work
go.work.sum
